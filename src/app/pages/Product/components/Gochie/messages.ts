import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
  title: () => _t(translations.productPage.Gochie.title),
  subtitle: () => _t(translations.productPage.Gochie.subtitle),
  subtitle2: () => _t(translations.productPage.Gochie.subtitl2),
};
