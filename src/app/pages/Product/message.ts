import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
  title: () => _t(translations.productPage.title),
  subtitle: () => _t(translations.productPage.subtitle),
};
