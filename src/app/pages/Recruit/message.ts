import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
  title: () => _t(translations.RecruitPage.Banner.Title),
  subtitle: () => _t(translations.RecruitPage.Banner.Subtitle),
};
