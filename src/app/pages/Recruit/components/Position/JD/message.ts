import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
  back: () => _t(translations.RecruitPage.Position.Back),
  apply: () => _t(translations.RecruitPage.Position.Apply),
};
