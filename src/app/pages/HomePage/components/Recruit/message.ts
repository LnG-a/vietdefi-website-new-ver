import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
  recruitTitle: () => _t(translations.HomePage.Recruit.title),
  recruitContent: () => _t(translations.HomePage.Recruit.content),
  recruitContact: () => _t(translations.HomePage.Recruit.contact),
  buttonTitle: () => _t(translations.HomePage.Recruit.buttonTitle),
};
